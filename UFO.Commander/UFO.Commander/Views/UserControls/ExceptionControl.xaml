<UserControl x:Class="UFO.Commander.Views.UserControls.ExceptionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:UFO.Commander.ViewModel"
             DataContext="{Binding ExceptionViewModel}"
             d:DataContext="{d:DesignInstance Type=vm:ExceptionViewModel, IsDesignTimeCreatable=True}"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="{Binding Title}" />
        <TextBlock Grid.Row="1" Text="{Binding Message}" />
        <Button Grid.Row="2" Content="Confirm" />
        <TextBlock Grid.Row="3" Text="{Binding Exception.StackTrace}" />
    </Grid>
</UserControl>
